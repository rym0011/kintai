// パスワード認証 ****************************************
function Enter(){

  let pass = prompt("ninsyo","");  // パスワード格納

  if(pass === ""){
  // 入力無し >> イベント無し
  }else if(pass === "aaa"){
  // パスワード一致 >> マイページ遷移
    location.href = "calendar.html";
  }else{
  // パスワード不一致 >> 認証NG表示
    alert("NG");
  }
}
// *****************************************************
